keyserver: "https://localhost:8443"
keyserver_port: 8813
access:
  - criteria:
      - server: {re: ".*"}
    permissions:
      - changeuser
      - login
      - noauth
    params:
      - expiryhours: 24
